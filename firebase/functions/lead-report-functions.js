const {onCall,HttpsError}=require('firebase-functions/v2/https');
const {defineSecret}=require('firebase-functions/params');
const {getFirestore,FieldValue}=require('firebase-admin/firestore');
const nodemailer=require('nodemailer');
const PDFDocument=require('pdfkit');
const {clean,num,round2,canonicalCategory,scoreRecord,buildDrivers,normalizeBonus}=require('./bonus-utils');
const db=getFirestore();
const REPORT_EMAIL=defineSecret('REPORT_EMAIL');
const REPORT_EMAIL_APP_PASSWORD=defineSecret('REPORT_EMAIL_APP_PASSWORD');
const DEFAULT_SCORING={rescueYes:-.2,rescuePositive:0,ncns:0,co:0,lateMorning:0,complaints:0,safety:0,pickups:0,dsb:0,dvic:0,otherInfra:0};
function fmt(v){const n=round2(v);return `${n>0?'+':''}${n.toFixed(2)}`;}
function idSafe(v){return clean(v).replace(/\//g,'_');}
async function settings(){const [s,b]=await Promise.all([db.collection('settings').doc('scoring').get(),db.collection('settings').doc('bonus').get()]);return{scoring:{...DEFAULT_SCORING,...(s.exists?s.data():{})},bonus:normalizeBonus(b.exists?b.data():{})};}
async function loadWeek(week){const snap=await db.collection('records').where('week','==',week).get();return snap.docs.map(d=>d.data());}
function findDriver(drivers,station,id){const x=clean(id).toUpperCase();return drivers.find(d=>d.station===station&&(clean(d.transporterId).toUpperCase()===x||clean(d.driverKey).toUpperCase()===x));}
function summarize(d){const rescue=d.records.filter(r=>r.kind==='RESCUE');return{packages:d.packages,base:d.overallScore,raw:d.rawScore,total:d.finalScore,rescues:rescue.length,rescueStops:rescue.reduce((a,r)=>a+num(r.extra?.stops),0),rescuePackages:rescue.reduce((a,r)=>a+num(r.extra?.packages),0),...d.counts,daysWorked:d.daysWorked,rescueDays:d.rescueDays,negativeRescuePoints:d.negativeRescuePoints};}
function bonusTitle(d){if(d.bonusWinner)return'QUALIFIED FOR BONUS';if(d.eligible)return'ELIGIBLE — OUTSIDE BONUS CUTOFF';return'NOT ELIGIBLE FOR BONUS';}
function bonusChecks(d,bonus){const checks=[];checks.push({label:'Days worked',value:`${d.daysWorked} day(s) · minimum ${num(bonus.minDaysWorked)}`,pass:!!d.schedulePresent&&d.daysWorked>=num(bonus.minDaysWorked)});checks.push({label:'Rescue Driver days',value:`${d.rescueDays} day(s) · maximum ${num(bonus.maxRescueDays)}`,pass:!!d.schedulePresent&&d.rescueDays<=num(bonus.maxRescueDays)});const defs=[['safety','Safety violations',d.counts.safety],['complaints','Complaints',d.counts.complaints],['ncns','NCNS',d.counts.ncns],['co','Call Out (CO)',d.counts.co],['lateMorning','Late Morning',d.counts.lateMorning],['pickups','Pickups',d.counts.pickups],['dsb','DSB',d.counts.dsb],['dvic','DVIC',d.counts.dvic]];for(const [key,label,value] of defs){const rule=bonus.rules?.[key];if(!rule?.enabled)continue;checks.push({label,value:`${value} · disqualifies at ${num(rule.threshold)}+`,pass:value<num(rule.threshold)});}const rr=bonus.rules?.negativeRescuePoints;if(rr?.enabled)checks.push({label:'Negative rescue points',value:`${d.negativeRescuePoints.toFixed(2)} · maximum ${num(rr.threshold).toFixed(2)}`,pass:d.negativeRescuePoints<=num(rr.threshold)});return checks;}
async function leadForDriver(transporterId){const tid=idSafe(transporterId);if(!tid)return null;const a=await db.collection('driverLeadAssignments').doc(tid).get();if(!a.exists)return null;const data=a.data()||{};if(data.leadId){const l=await db.collection('leads').doc(data.leadId).get();if(l.exists){const ld=l.data()||{};if(clean(ld.email))return{id:l.id,name:clean(ld.name)||'LEAD',email:clean(ld.email)};}}if(clean(data.leadEmail))return{id:clean(data.leadId),name:clean(data.leadName)||'LEAD',email:clean(data.leadEmail)};return null;}
function buildPdf({d,email,week,scoring,bonus,lead}){const summary=summarize(d);return new Promise((resolve,reject)=>{const doc=new PDFDocument({size:'LETTER',margin:48,info:{Title:`AAXI Xpress Driver Report ${week}`,Author:'AAXI Xpress'}}),chunks=[];doc.on('data',c=>chunks.push(c));doc.on('end',()=>resolve(Buffer.concat(chunks)));doc.on('error',reject);
const line=()=>{doc.strokeColor('#D0D5DD').moveTo(48,doc.y).lineTo(564,doc.y).stroke();doc.moveDown(.7);};
doc.font('Helvetica-Bold').fontSize(24).fillColor('#101828').text('AAXI Xpress');doc.font('Helvetica').fontSize(10).fillColor('#667085').text('Weekly Driver Performance Report');doc.moveDown(.6);line();doc.font('Helvetica-Bold').fontSize(16).fillColor('#101828').text(d.name||'Driver');doc.font('Helvetica').fontSize(9).fillColor('#667085').text(`Transporter ID: ${d.transporterId||'-'}   |   Station: ${d.station}   |   Week: ${week}`);if(email)doc.text(`Email: ${email}`);if(lead)doc.text(`Coaching LEAD: ${lead.name}`);
doc.moveDown(1);const y0=doc.y;doc.roundedRect(48,y0,516,64,8).fillAndStroke('#F8FAFC','#E4E7EC');doc.fillColor('#344054').font('Helvetica-Bold').fontSize(9).text('PACKAGES DELIVERED',64,y0+14).fontSize(20).fillColor('#101828').text(String(summary.packages),64,y0+31);doc.fontSize(9).fillColor('#344054').text('DAYS WORKED',250,y0+14).fontSize(20).fillColor('#101828').text(String(summary.daysWorked),250,y0+31);doc.fontSize(9).fillColor('#344054').text('RESCUE DRIVER DAYS',405,y0+14).fontSize(20).fillColor('#101828').text(String(summary.rescueDays),405,y0+31);doc.y=y0+84;
doc.font('Helvetica-Bold').fontSize(13).fillColor('#101828').text('Performance summary');doc.moveDown(.5);const metrics=[['Packages Delivered',summary.packages],['Days Worked',summary.daysWorked],['Rescue Driver Days',summary.rescueDays],['Rescues',summary.rescues],['Complaints',summary.complaints],['Safety',summary.safety],['NCNS',summary.ncns],['Call Out (CO)',summary.co],['Late Morning',summary.lateMorning],['Pickups',summary.pickups],['DSB',summary.dsb],['DVIC',summary.dvic]];let y=doc.y;metrics.forEach((m,i)=>{const x=i%2===0?48:306;if(i%2===0&&i>0)y+=25;doc.font('Helvetica').fontSize(9).fillColor('#667085').text(m[0],x,y,{width:150});doc.font('Helvetica-Bold').fillColor('#101828').text(String(m[1]),x+165,y,{width:70,align:'right'});});doc.y=y+45;
doc.font('Helvetica-Bold').fontSize(13).fillColor('#101828').text('Report details');doc.moveDown(.5);const rowsDetail=[];for(const r of d.records.filter(x=>!['OVERVIEW','WORK_SCHEDULE','MANUAL_OVERRIDE'].includes(x.kind))){if(r.kind==='LOG_INFRA'&&!['NCNS','CO','LATE_MORNING'].includes(canonicalCategory(r.extra?.category||r.label)))continue;let label=r.kind||'Event',detail=clean(r.label);if(r.kind==='INFRACTION'){label='INFRACTION';detail=`Safety Violation · ${clean(r.extra?.metricType||r.label)||'Safety Violation'}`;}else if(r.kind==='RESCUE'){label='RESCUE';detail=`Stops ${num(r.extra?.stops)} · Packages ${num(r.extra?.packages)} · Affects ${clean(r.extra?.affects)||'-'}`;}else if(r.kind==='COMPLAINT'){label='COMPLAINT';detail=clean(r.extra?.details||r.label)||'Customer Delivery Feedback';}else if(r.kind==='LOG_INFRA'){const c=canonicalCategory(r.extra?.category||r.label);label=c==='LATE_MORNING'?'LATE MORNING':c;detail='LOG · INFRA_LOG';}rowsDetail.push({label,date:clean(r.date),detail});}if(!rowsDetail.length){doc.font('Helvetica').fontSize(9).fillColor('#667085').text('No reportable events for this week.');doc.moveDown(1);}for(const r of rowsDetail){if(doc.y>705)doc.addPage();const yy=doc.y;doc.strokeColor('#EAECF0').moveTo(48,yy).lineTo(564,yy).stroke();doc.moveDown(.55);doc.font('Helvetica-Bold').fontSize(9).fillColor('#101828').text(r.label,48,doc.y,{width:155});doc.font('Helvetica').fontSize(8).fillColor('#667085').text(`${r.date?`${r.date} · `:''}${r.detail||''}`,205,doc.y,{width:359});doc.y+=22;}
if(doc.y>640)doc.addPage();doc.moveDown(.8);line();if(lead?.email){const subject=encodeURIComponent(`Question about weekly driver report ${week} - ${d.name}`),body=encodeURIComponent(`Hello ${lead.name},\n\nI have a question about my AAXI Xpress weekly performance report for ${week}.\n\nMy name: ${d.name}\nTransporter ID: ${d.transporterId||'-'}\nStation: ${d.station}\n\nMy question:\n`),mailto=`mailto:${lead.email}?subject=${subject}&body=${body}`,by=doc.y;doc.font('Helvetica').fontSize(8).fillColor('#667085').text(`Questions about this report? Your coaching LEAD is ${lead.name}.`,48,by,{width:516,align:'center'});doc.roundedRect(178,by+18,256,34,7).fill('#067647');doc.font('Helvetica-Bold').fontSize(11).fillColor('#FFFFFF').text('ASK MY LEAD',178,by+29,{width:256,align:'center'});doc.link(178,by+18,256,34,mailto);doc.y=by+66;}else{doc.font('Helvetica').fontSize(8).fillColor('#667085').text('No coaching LEAD is assigned yet. Contact your dispatcher if you have questions about this report.',48,doc.y,{width:516,align:'center'});doc.moveDown(1.2);}doc.font('Helvetica').fontSize(8).fillColor('#98A2B3').text('AAXI Xpress · Weekly driver performance report.',48,doc.y,{width:516,align:'center'});doc.end();});}
function mailer(){const user=REPORT_EMAIL.value(),pass=REPORT_EMAIL_APP_PASSWORD.value();if(!user||!pass)throw new HttpsError('failed-precondition','El correo remitente todavía no está configurado.');return{user,tx:nodemailer.createTransport({service:'gmail',auth:{user,pass}})};}
async function renderOne({week,station,transporterId,requireEmail}){const [records,cfg]=await Promise.all([loadWeek(week),settings()]);const drivers=buildDrivers(records,cfg.scoring,cfg.bonus),d=findDriver(drivers,station,transporterId);if(!d)throw new HttpsError('not-found','No encontré el reporte del driver para esa semana.');const tid=d.transporterId||transporterId;const [dir,lead]=await Promise.all([db.collection('driverDirectory').doc(tid).get(),leadForDriver(tid)]),email=clean(dir.exists?dir.data()?.email:'');if(requireEmail&&!email)throw new HttpsError('failed-precondition','Este driver no tiene email guardado en Directorio.');const pdf=await buildPdf({d,email,week,scoring:cfg.scoring,bonus:cfg.bonus,lead});return{pdf,d,email,lead};}
async function sendOne(args){const {pdf,d,email,lead}=await renderOne({...args,requireEmail:true});const {user,tx}=mailer();await tx.sendMail({from:`\"AAXI Xpress\" <${user}>`,to:email,replyTo:'no-reply@aaxixpress.invalid',headers:{'Auto-Submitted':'auto-generated'},subject:`AAXI Xpress · Weekly Driver Report · ${args.week}`,html:`<div style=\"font-family:Arial,sans-serif;color:#101828;line-height:1.55\"><h2>AAXI Xpress</h2><p>Hello ${d.name},</p><p>Your weekly performance report for <b>${args.week}</b> at <b>${d.station}</b> is attached.</p><p><b>Final score: ${d.finalScore.toFixed(2)} / 100</b></p><p><b>Bonus status: ${bonusTitle(d)}</b></p>${lead?`<p>Your coaching LEAD is <b>${lead.name}</b>. If you have a question, use the <b>ASK MY LEAD</b> button at the end of the PDF.</p>`:''}<p style=\"color:#667085;font-size:12px\">This is an automated report. Replies to this email are not monitored. Use the button inside the PDF for questions.</p></div>`,attachments:[{filename:`AAXI_Xpress_${args.week}_${d.transporterId}.pdf`,content:pdf,contentType:'application/pdf'}]});await db.collection('reportSends').doc(`${args.week}_${d.station}_${d.transporterId}`).set({week:args.week,station:d.station,transporterId:d.transporterId,email,leadId:lead?.id||'',leadEmail:lead?.email||'',sent:true,sentAt:FieldValue.serverTimestamp(),delivery:'email_pdf'},{merge:true});return{ok:true,email,name:d.name,total:d.finalScore,bonusStatus:d.bonusStatus,lead:lead?.name||''};}
function validate(data){const week=clean(data?.week),station=clean(data?.station).toUpperCase(),transporterId=clean(data?.transporterId);if(!/^\d{4}-W\d{2}$/.test(week)||!['DJX3','DJX4'].includes(station)||!transporterId)throw new HttpsError('invalid-argument','Datos del reporte inválidos.');return{week,station,transporterId};}
exports.previewDriverReport=onCall({region:'us-east1',timeoutSeconds:120,memory:'512MiB'},async req=>{if(!req.auth)throw new HttpsError('unauthenticated','Acceso no preparado.');const a=validate(req.data),{pdf,d,email,lead}=await renderOne({...a,requireEmail:false});return{ok:true,name:d.name,email,total:d.finalScore,bonusStatus:d.bonusStatus,lead:lead?.name||'',fileName:`AAXI_Xpress_${a.week}_${d.transporterId}.pdf`,pdfBase64:pdf.toString('base64')};});
exports.sendDriverReport=onCall({region:'us-east1',timeoutSeconds:120,memory:'512MiB',secrets:[REPORT_EMAIL,REPORT_EMAIL_APP_PASSWORD]},async req=>{if(!req.auth)throw new HttpsError('unauthenticated','Acceso no preparado.');return sendOne(validate(req.data));});
exports.sendStationReports=onCall({region:'us-east1',timeoutSeconds:900,memory:'1GiB',secrets:[REPORT_EMAIL,REPORT_EMAIL_APP_PASSWORD]},async req=>{if(!req.auth)throw new HttpsError('unauthenticated','Acceso no preparado.');const week=clean(req.data?.week),station=clean(req.data?.station).toUpperCase();if(!/^\d{4}-W\d{2}$/.test(week)||!['DJX3','DJX4'].includes(station))throw new HttpsError('invalid-argument','Semana o estación inválida.');const [records,cfg]=await Promise.all([loadWeek(week),settings()]),drivers=buildDrivers(records,cfg.scoring,cfg.bonus).filter(d=>d.station===station);let sent=0;const failed=[];for(const d of drivers){try{await sendOne({week,station,transporterId:d.transporterId||d.driverKey});sent++;}catch(err){failed.push({transporterId:d.transporterId,error:err.message||String(err)});}}return{ok:true,total:drivers.length,sent,failed:failed.length,failures:failed.slice(0,20)};});